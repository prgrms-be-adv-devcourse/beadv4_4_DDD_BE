name: CD Pipeline

on:
  push:
    branches: [main, feature/26]

permissions:
  contents: read

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build with Gradle
        run: ./gradlew build -x test

      - name: Docker login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/modeunsa:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/modeunsa:latest

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_KEY }}
          script: |
            # 네트워크 생성 (없으면)
            docker network create modeunsa-net || true
            # Redis 실행 (없으면)
            docker ps | grep redis || docker run -d --name redis --network modeunsa-net -p 6379:6379 redis:7-alpine
            # 앱 배포
            docker pull ${{ secrets.DOCKER_USERNAME }}/modeunsa:latest
            docker stop modeunsa || true
            docker rm modeunsa || true
            docker run -d --name modeunsa --network modeunsa-net -p 8080:8080 -e REDIS_HOST=redis ${{ secrets.DOCKER_USERNAME }}/modeunsa:latest
            docker image prune -f
